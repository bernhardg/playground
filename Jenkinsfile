#!groovy
node {
    stage('Build') {
        echo "Doing Build..."
        echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
    }
    
    stage('Unit Test') {
        echo "Doing Unit Testing..."
    }
    
    stage('Deploy') {
        echo "Doing Deployment..."
    }
}
